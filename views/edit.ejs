<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>編集 | 日報保管庫</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header class="app-header">
      <div class="brand">
        <div class="brand-title">日報保管庫</div>
        <div class="brand-sub">日報編集</div>
      </div>
      <a class="button" href="/reports/<%= report.id %>">詳細へ戻る</a>
    </header>

    <main class="container">
      <section class="form-card">
        <% if (error) { %>
          <div class="error"><%= error %></div>
        <% } %>
        <form action="/reports/<%= report.id %>" method="POST" enctype="multipart/form-data" class="report-form">
          <label>
            日付
            <input type="date" name="date" value="<%= report.date || '' %>" />
          </label>
          <label>
            都道府県
            <input type="text" name="prefecture" placeholder="例: 東京都" value="<%= report.prefecture || '' %>" />
          </label>
          <label>
            会社名
            <input type="text" name="company" placeholder="例: 株式会社サンプル" value="<%= report.company || '' %>" />
          </label>
          <label>
            タイトル
            <input type="text" name="title" placeholder="例: 週次報告" value="<%= report.title || '' %>" />
          </label>
          <label>
            本文
            <textarea name="body" rows="8" placeholder="本文を入力"><%= report.body || '' %></textarea>
          </label>
          <label>
            タグ (カンマ区切り)
            <input type="text" name="tags" placeholder="例: 営業, 進捗" value="<%= report.tags || '' %>" />
          </label>

          <div class="attachments-edit">
            <div class="section-title">既存添付</div>
            <% if (attachments.length === 0) { %>
              <div class="empty">添付ファイルはありません。</div>
            <% } else { %>
              <div class="attachment-grid">
                <% attachments.forEach((file) => { %>
                  <label class="attachment-card selectable">
                    <input type="checkbox" name="deleteAttachments" value="<%= file.id %>" />
                    <div class="attachment-preview">
                      <% if (file.mime_type.startsWith('image/')) { %>
                        <img src="/uploads/<%= file.filename %>" alt="<%= file.original_name %>" />
                      <% } else { %>
                        <div class="pdf-label">PDF</div>
                      <% } %>
                    </div>
                    <div class="attachment-name"><%= file.original_name %></div>
                    <div class="attachment-delete">削除</div>
                  </label>
                <% }) %>
              </div>
            <% } %>
          </div>

          <label>
            添付ファイルを追加 (jpg / png / pdf)
            <input type="file" name="files" multiple accept=".jpg,.jpeg,.png,.pdf" />
          </label>
          <div class="actions">
            <button type="submit" class="button primary">更新する</button>
          </div>
        </form>
      </section>
    </main>
  </body>
</html>
